<h1>Edit Review for Workspace: <%= @review.workspace.name %></h1>

<%= simple_form_for @review do |f| %>

  <%= f.input :title %>

  <%= f.input :detail %>

  <%= f.input :rating, as: :select, collection: [1, 2, 3, 4, 5] %>

  <%= f.button :submit %>
<% end %>